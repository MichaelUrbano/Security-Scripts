name: Tests

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  test:
    name: Run BATS tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        distro:
          - 'debian:latest'
          - 'ubuntu:latest'
          - 'oraclelinux:10'
          - 'fedora:latest'
          - 'opensuse/leap:latest'
    container:
      image: ${{ matrix.distro }}
      options: --privileged --cap-add=NET_ADMIN

    steps:
      - name: Install tar for checkout
        if: contains(matrix.distro, 'opensuse') || contains(matrix.distro, 'oraclelinux') || contains(matrix.distro, 'fedora')
        run: |
          if command -v zypper >/dev/null; then zypper refresh && zypper install -y tar gzip sudo; fi
          if command -v dnf >/dev/null; then dnf install -y tar gzip sudo; fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (Debian/Ubuntu)
        if: contains(matrix.distro, 'debian') || contains(matrix.distro, 'ubuntu')
        run: |
          apt-get update
          apt-get install -y bats expect sudo iptables iproute2 net-tools

      - name: Install dependencies (Oracle/Fedora)
        if: contains(matrix.distro, 'oraclelinux') || contains(matrix.distro, 'fedora')
        run: |
          dnf install -y oracle-epel-release-el10 || true
          dnf update -y
          dnf install -y bats expect sudo iptables iproute2 iproute net-tools tar gzip --allowerasing || dnf install -y bats expect sudo iptables iproute net-tools tar gzip

      - name: Install dependencies (OpenSUSE)
        if: contains(matrix.distro, 'opensuse')
        run: |
          zypper refresh
          zypper install -y bats expect sudo iptables iproute2 net-tools tar gzip

      - name: Run BATS tests
        run: |
          cd tests/Linux/harden
          bats test_tui.bats
          bats test_permissions.bats
